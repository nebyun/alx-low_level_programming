#include "main.h"
/**
 * _isdigit - check discription
 * @c: intiger value
 * Return: 1 if uppercase or 0 otherwise
 */
int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
		return (1);
	else
		return (0);
}
